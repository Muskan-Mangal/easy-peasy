#include<bits/stdc++.h>
using namespace std;
int main()
{
	freopen("Untitled1.cpp","r",stdin);
	//i'm taking printing this whole file as o/p
	char ch;
	int count=0;
//	while( cin>>ch && ch!= EOF)
//		cout<<ch;

 while( (ch=getchar())!= EOF)
		cout<<ch;
	fclose(stdin);
}

